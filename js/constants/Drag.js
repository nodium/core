(function (context, undefined) {

    // 'use strict';

    var constants   = context.setNamespace('app.constants');

    const Drag = {
        LEFT: 1,
        RIGHT: 2,
        UP: 3,
        DOWN: 4
    };

    constants.Drag = Drag;

}(this));
